- name: Provision server
  hosts: all
  become: yes
  
  roles:
    - common
    - users
    - docker
    - project

  handlers:
    - name: restart ssh
      service:
        name: "{{ 'sshd' if ansible_os_family == 'RedHat' else 'ssh' }}"
        state: restarted